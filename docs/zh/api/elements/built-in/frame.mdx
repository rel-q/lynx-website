import { Required, Deprecated, Go } from '@lynx';

# `<frame>`

一个类似 HTML `<iframe>` 的页面元素，可以将一个 Lynx 页面迁入当前页面中。

:::tip
`<frame>` 元件于 Lynx 3.4 版本引入，需要使用该元件的场景，需将依赖的 Lynx 版本升级至 3.4 及以上。
:::

:::caution
`<frame>` 暂不支持自动撑高，需要手动设置 frame 的宽高。
:::

import APIReference from './frame-API.mdx';

<APIReference />

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="elements/frame" />
