# isValid

确定当前的 `TemplateBundle` 对象是否有效。

## 语法

### Android

```java
public boolean isValid();
```

#### 返回值

- 如果当前的 `TemplateBundle` 对象有效，则为 `true`，否则为 `false`。

## 兼容性

import { APITable } from '@lynx';

<APITable query="lynx-native-api/template-bundle/is-valid" />