# sendGlobalEvent

通过 `LynxBackgroundRuntime` 向前端发送全局事件，前端可以通过 `GlobalEventEmitter` 监听该事件。

## 语法

### Android

```java
public void sendGlobalEvent(String name, JavaOnlyArray params);
```

#### 参数说明

- `name`: 全局事件名。
- `params`: 全局事件携带的参数。

### iOS

```objc
- (void)sendGlobalEvent:(nonnull NSString *)name withParams:(nullable NSArray *)params;
```

#### 参数说明

- `name`: 全局事件名。
- `params`: 全局事件携带的参数。

### Harmony

```ts
runtime.sendGlobalEvent('eventName', []);
```

## 兼容性

import { APITable } from '@lynx';

<APITable query="lynx-native-api/lynx-background-runtime/send-global-event" />
